---
# Combined Ingress for Backend API and Frontend Dashboard
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: marketplace-ingress
  namespace: marketplace
  labels:
    app: marketplace
    tier: ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "30"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "30"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "30"
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "http://localhost:3000,https://vendor.marketplace.com,https://dashboard.vendor.marketplace.com"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, PATCH, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"
spec:
  tls:
    - hosts:
        - api.vendor.marketplace.com
        - vendor-api.marketplace.com
        - vendor.marketplace.com
        - dashboard.vendor.marketplace.com
      secretName: marketplace-tls
  rules:
    # ============================================
    # Backend API Routes
    # ============================================
    
    # Vendor API Endpoints
    - host: api.vendor.marketplace.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vendor-service
                port:
                  number: 8080
    
    # Alternative backend domain
    - host: vendor-api.marketplace.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vendor-service
                port:
                  number: 8080
    
    # ============================================
    # Frontend Dashboard Routes
    # ============================================
    
    # Main dashboard domain
    - host: vendor.marketplace.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vendor-dashboard
                port:
                  number: 80
    
    # Alternative dashboard domain
    - host: dashboard.vendor.marketplace.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vendor-dashboard
                port:
                  number: 80
    
    # ============================================
    # Combined Localhost Access
    # ============================================
    
    - host: localhost
      http:
        paths:
          # Backend API
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: vendor-service
                port:
                  number: 8080
          
          # Frontend Dashboard
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vendor-dashboard
                port:
                  number: 80

---
# TLS Certificate for Ingress
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: marketplace-cert
  namespace: marketplace
  labels:
    app: marketplace
spec:
  secretName: marketplace-tls
  duration: 2160h  # 90 days
  renewBefore: 720h  # 30 days
  commonName: vendor.marketplace.com
  dnsNames:
    - api.vendor.marketplace.com
    - vendor-api.marketplace.com
    - vendor.marketplace.com
    - dashboard.vendor.marketplace.com
    - "*.vendor.marketplace.com"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

---
# ClusterIssuer for Let's Encrypt
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
  labels:
    app: marketplace
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: admin@marketplace.com
    privateKeySecretRef:
      name: letsencrypt-prod-key
    solvers:
      - http01:
          ingress:
            class: nginx

---
# NetworkPolicy for Backend Service Ingress
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vendor-service-ingress-netpol
  namespace: marketplace
  labels:
    app: vendor-service
spec:
  podSelector:
    matchLabels:
      app: vendor-service
      tier: backend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow from nginx ingress controller
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
          podSelector:
            matchLabels:
              app.kubernetes.io/name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
    
    # Allow from frontend dashboard
    - from:
        - podSelector:
            matchLabels:
              app: vendor-dashboard
              tier: frontend
      ports:
        - protocol: TCP
          port: 8080
    
    # Allow from within same namespace
    - from:
        - podSelector: {}
      ports:
        - protocol: TCP
          port: 8080
  
  egress:
    # Allow to database (vendor-db)
    - to:
        - podSelector:
            matchLabels:
              app: vendor-db
              tier: database
      ports:
        - protocol: TCP
          port: 5432
    
    # Allow to Redis
    - to:
        - podSelector:
            matchLabels:
              app: redis
              tier: cache
      ports:
        - protocol: TCP
          port: 6379
    
    # Allow DNS
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
    
    # Allow to external APIs (0.0.0.0/0 on port 443)
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
            except:
              - 169.254.169.254/32  # Block AWS metadata service
      ports:
        - protocol: TCP
          port: 443
